namespace WPFSampleApplication.UserInterface.Abstractions;

/// <summary>
/// Base class of all store type classes.
/// It`s empty now but the idea is to have a base class per role
/// in order to have a better control on classes with same role.
/// </summary>
public abstract class StoreBase { }
